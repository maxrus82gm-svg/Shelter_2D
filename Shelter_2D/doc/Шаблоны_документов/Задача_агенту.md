# Задача_агенту

Рабочий документ активной задачи проекта `{{PROJECT_NAME}}`.

## Связи Obsidian

- [[Старт]]
- [[Список_правил]]
- [[Задача_внешнему_субагенту]]

---

# БЛОК 1 — ТЕКУЩАЯ ЗАДАЧА

## Цель

Сделать чистый первый этап подгонки агентской документации под реальный проект `Shelter_2D`.

На этом этапе нужно:

- прочитать обязательные документы переноса;
    
- заполнить или проверить `00_Паспорт_проекта.md`;
    
- зафиксировать реальные пути, репозиторий и структуру проекта;
    
- проверить, какие шаблоны и рабочие документы есть;
    
- подготовить понятный отчёт и план следующего этапа.
    

На этом этапе **не выполнять массовую замену placeholders во всех шаблонах** и **не создавать рабочие документы из шаблонов**, чтобы не запутать структуру.

---

## Обязательный порядок чтения

Перед любыми действиями прочитать строго в таком порядке:

```text
m:\GitHub\Shelter_2D\Shelter_2D\doc\README_как_переносить.md
m:\GitHub\Shelter_2D\Shelter_2D\doc\MANIFEST_структуры.md
m:\GitHub\Shelter_2D\Shelter_2D\doc\00_Паспорт_проекта.md
m:\GitHub\Shelter_2D\Shelter_2D\doc\02_Карта_переменных.md
m:\GitHub\Shelter_2D\Shelter_2D\doc\01_Правило_подгонки_под_новый_проект.md
m:\GitHub\Shelter_2D\Shelter_2D\doc\03_Чеклист_проверки_переноса.md
```

Если какой-то обязательный файл не найден — остановиться, ничего не менять и указать проблему в `PROBLEMS`.

---

## Реальная структура проекта

Пользователь подтвердил, что рабочая база Obsidian и проект находятся здесь:

```text
m:\GitHub\Shelter_2D\Shelter_2D
```

Текущая структура:

```text
m:\GitHub\Shelter_2D\Shelter_2D
├─ .gigacode
├─ .obsidian
├─ doc
├─ Shelter_2D_Game
├─ Локальный агент
└─ Облачный агент
```

Папка документации:

```text
m:\GitHub\Shelter_2D\Shelter_2D\doc
```

---

## Данные для паспорта проекта

Проверить и заполнить `00_Паспорт_проекта.md` следующими значениями:

```text
PROJECT_NAME: Shelter_2D
PROJECT_ROOT: m:\GitHub\Shelter_2D\Shelter_2D
OBSIDIAN_VAULT: m:\GitHub\Shelter_2D\Shelter_2D
DOC_FOLDER: m:\GitHub\Shelter_2D\Shelter_2D\doc
REPOSITORY_URL: https://github.com/maxrus82gm-svg/Shelter_2D.git
REPOSITORY_TYPE: GitHub
MAIN_TASK_FILE: НЕ УКАЗАНО
EXTERNAL_AGENT_TASK_FILE: НЕ УКАЗАНО
START_FILE: m:\GitHub\Shelter_2D\Shelter_2D\doc\Шаблоны_документов\Старт.md
RULES_FOLDER: m:\GitHub\Shelter_2D\Shelter_2D\doc
PROJECT_TYPE: C++ 2D game / SFML
BUILD_SYSTEM: Visual Studio 2022 / MSBuild
CODE_FOLDERS: Shelter_2D_Game; Shelter_2D_Game\src
ASSET_FOLDERS: НЕ УКАЗАНО
CONFIG_FOLDERS: НЕ УКАЗАНО
FORBIDDEN_FOLDERS: .git; .vs; .obsidian; .gigacode; bin; obj; SFML-2.6.1; Локальный агент; Облачный агент
AGENT_CONFIG_PATHS: НЕ УКАЗАНО
SUBAGENT_PROFILES: НЕ УКАЗАНО
MAIN_AGENT_NAME: основной агент
LOCAL_AGENT_NAME: локальный агент
EXTERNAL_AGENT_NAME: внешний агент через чат пользователя
LOCAL_AGENT_ROUTE: папка `Локальный агент`, если пользователь отдельно разрешит
EXTERNAL_AGENT_ROUTE: пользователь передаёт малый блок во внешний чат агента
SERVICE_FOLDERS: .git; .vs; .obsidian; .gigacode; bin; obj; Локальный агент; Облачный агент
IDE_SERVICE_FILES: .vs/*; *.suo; *.user; *.vcxproj.user
CURRENT_BASE_COMMIT: НЕ УКАЗАНО
USER_GIT_POLICY: Git контролирует пользователь. Агент не делает commit, push, pull, merge, rebase, reset, clean, checkout, switch или изменение Git-конфига без прямой команды пользователя.
```

Если агент уверен, что `MAIN_TASK_FILE` уже существует, указать реальный путь.  
Если не уверен — оставить `НЕ УКАЗАНО`, не выдумывать.

---

## Правило языка и имён документов

Все новые рабочие пользовательские Markdown-документы должны называться на русском языке.

Внутри рабочих документов текст должен быть на русском языке.

Не переименовывать технические и служебные файлы без прямого разрешения пользователя.

Технические имена допустимы для уже существующих служебных файлов и форматов:

```text
README_как_переносить.md
MANIFEST_структуры.md
AGENTS.md
.gitignore
.sln
.vcxproj
.obsidian
.gigacode
```

---

## Что нужно сделать на этом этапе

1. Прочитать обязательные документы в правильном порядке.
    
2. Проверить `00_Паспорт_проекта.md`.
    
3. Заполнить в паспорте только реальные известные значения.
    
4. Для неизвестных значений писать `НЕ УКАЗАНО`.
    
5. Не подставлять догадки.
    
6. Проверить, какие документы и папки реально существуют внутри:
    

```text
m:\GitHub\Shelter_2D\Shelter_2D\doc
```

7. Отдельно определить:
    

- есть ли рабочий `Задача_агенту.md`;
    
- есть ли рабочий `Старт.md` вне папки шаблонов;
    
- есть ли рабочий `Список_правил.md`;
    
- есть ли папка `Шаблоны_документов`;
    
- есть ли папка `Формы_отчётов`;
    
- какие документы пока являются шаблонами, а какие уже рабочими.
    

8. Подготовить план Этапа 2:
    

- какие рабочие документы нужно создать из шаблонов;
    
- какие документы нужно переименовать в русские названия;
    
- какие placeholders нужно будет заменить;
    
- какие файлы нельзя трогать;
    
- какие вопросы нужно уточнить у пользователя.
    

---

## Что нельзя делать

На этом этапе нельзя:

- массово заменять placeholders во всех шаблонах;
    
- создавать рабочие документы из шаблонов;
    
- переносить файлы;
    
- переименовывать файлы;
    
- менять код игры;
    
- менять `.sln`, `.vcxproj`, `.filters`, `.user`;
    
- менять `.git`;
    
- менять `.obsidian`;
    
- менять `.gigacode`;
    
- менять `Shelter_2D_Game`;
    
- менять `Локальный агент`;
    
- менять `Облачный агент`;
    
- делать commit, push, pull, merge, rebase, reset, clean, checkout, switch;
    
- менять Git-конфиг.
    

Разрешено изменить только:

```text
m:\GitHub\Shelter_2D\Shelter_2D\doc\00_Паспорт_проекта.md
```

Если для выполнения задачи нужно изменить другой файл, сначала указать это в `PROBLEMS` и не менять его без разрешения пользователя.

---

## Проверки

После работы проверить:

- паспорт заполнен известными значениями;
    
- неизвестные значения помечены как `НЕ УКАЗАНО`;
    
- обязательный порядок чтения соблюдён;
    
- структура документации описана;
    
- код проекта не менялся;
    
- служебные папки не менялись;
    
- Git-история не менялась.
    

Разрешены только безопасные Git-проверки чтения:

```text
git status --short --untracked-files=all
git diff --name-only
git diff --check
```

Если Git-проверки не выполнялись, так и написать в отчёте.

---

## Формат отчёта

В конце дать компактный отчёт:

```text
DONE:
- что сделано

CHANGED:
- намеренно изменённые файлы

UNEXPECTED CHANGES:
- побочные или автоматические изменения

UNTRACKED:
- новые неотслеживаемые файлы

CHECKS:
- что проверено

GIT:
- commit не создавался агентом
- push не выполнялся агентом
- pull не выполнялся агентом
- ветки не создавались и не переключались агентом
- Git-история не изменялась агентом

PROBLEMS:
- реальные проблемы или риски

NEXT:
- план Этапа 2
```

Если задача выполнена успешно, `БЛОК 1 — ТЕКУЩАЯ ЗАДАЧА` не очищать автоматически, потому что это первый этап переноса и требуется решение пользователя по Этапу 2.

В `NEXT` явно указать, что нужно подтвердить перед Этапом 2.

---

# БЛОК 2 — ПОСЛЕДНИЙ ОТЧЁТ АГЕНТА

Отчёта пока нет.

---

# БЛОК 3 — REVIEW / STATUS

## Большой блок

Большой блок хранится в `{{MAIN_TASK_FILE}}`, содержит цель, контекст, scope, разрешённые файлы, запреты, проверки и формат отчёта.

## Малый блок

Малый блок отправляется агенту в чат и дополняет большой блок поздними уточнениями.

Если малый блок противоречит большому, агент останавливается и пишет конфликт в `PROBLEMS`.

Прямое сообщение пользователя имеет приоритет над задачей второго агента.

## Review после отчёта

После отчёта агента основной review начинается с:

```text
СТАТУС КОММИТА
GIT / REPOSITORY CHECK
```

Если GitHub недоступен, писать `message не подтверждён`.

Если изменения локальные и не запушены, писать `push не обнаружен`.

## Контроль размещения активной задачи

- задача основного агента пишется в `{{MAIN_TASK_FILE}}`;
- задача внешнего агента пишется в `{{EXTERNAL_AGENT_TASK_FILE}}`;
- если в `{{MAIN_TASK_FILE}}` найден заголовок `ТЕКУЩАЯ ЗАДАЧА ВНЕШНЕМУ СУБАГЕНТУ`, это ошибка структуры;
- если внешний агент получил короткое сообщение, но `БЛОК 1` внешнего документа пустой, запуск считается неподготовленным;
- основной агент не должен имитировать внешний запуск и не должен писать отчёт от имени внешнего агента.
