# БОЛЬШОЙ БЛОК ЗАДАЧИ

```text
# БЛОК 1 — ТЕКУЩАЯ ЗАДАЧА

# TASK XXX {{PROJECT_NAME}} — Название задачи

Статус:
Новая активная задача.

Исполнитель:

Тип задачи:

Сложность:

Субагенты:

КОНТРОЛЬ МАРШРУТА АГЕНТОВ:
- Внутренние агенты Codex: `explorer`, `worker`, `default` и другие Codex-native роли. Это не внешние агенты.
- Локальные агенты: запускаются из Codex, но не являются внутренними агентами Codex; пример `simple_local / qwen3-coder:30b / локальный`.
- Внешние агенты: запускаются из других сред, не из Codex; задачу им передаёт пользователь через отдельный чат.
- Если требуется внешний агент, основной Codex не запускает `explorer`, `worker`, `default` или `simple_local`, а готовит внешний маршрут через `Задача_внешнему_субагенту.md`.

Git:

## 1. Главная цель

## 2. Рабочий проект

## 3. Что прочитать

## 4. Разрешено менять

## 5. Запрещено

## 6. Что именно сделать

## 7. Проверки перед завершением

## 8. Формат отчёта

## 9. Очистка БЛОКА 1
```

