# EXTERNAL_AGENT_TASK

Рабочий документ для постановки задач внешнему или локальному агенту через управляемый чат пользователя. Примеры: `GigaChat / VS Code`, `qwen3-coder:480b-cloud / OpenCode Desktop`, `qwen3-coder:30b / OpenCode Desktop`.

## Связи Obsidian

- [[Правила_использования_субагентов]]
- [[Настройка_прав_субагентов]]

---

# БЛОК 1 — ТЕКУЩАЯ ЗАДАЧА ВНЕШНЕМУ СУБАГЕНТУ

TASK EXT 012 Shelter_2D — read-only анализ старых путей после переноса игры

РЕЖИМ:
- read-only аналитика внешним агентом через чат пользователя

ПОДГОТОВИЛ:
- основной Codex

РАЗДАТЧИК:
- пользователь после проверки и возможной правки задачи

КОНТРОЛЬ МАРШРУТА АГЕНТОВ:
- Внутренние агенты Codex: `explorer`, `worker`, `default` и другие Codex-native роли. Это не внешние агенты.
- Локальные агенты: запускаются из Codex, но не являются внутренними агентами Codex; пример `simple_local / qwen3-coder:30b / локальный`.
- Внешние агенты: запускаются из других сред, не из Codex; задачу им передаёт пользователь через отдельный чат.
- Эта задача предназначена внешнему агенту через чат пользователя.
- Основной Codex не запускает `explorer`, `worker`, `default` или `simple_local` для этой задачи.

РАБОЧАЯ ЗОНА:
- `M:\GitHub\Shelter_2D`

СТАРЫЙ ПУТЬ:
- `M:\GitHub\Shelter_2D\Shelter_2D_Game`

НОВЫЙ ПУТЬ:
- `M:\GitHub\Shelter_2D\Shelter_2D\Shelter_2D_Game`

ФАЙЛ ОТЧЁТА:
- `M:\GitHub\Shelter_2D\Shelter_2D\doc\EXTERNAL_AGENT_TASK.md`
- Полный отчёт писать только в `БЛОК 2 — РАСШИРЕННЫЙ ОТЧЁТ ВНЕШНЕГО СУБАГЕНТА`.

ЦЕЛЬ:
- Найти упоминания старого расположения игры после переноса в новую папку.
- Определить, какие упоминания нужно исправить, какие оставить как исторические, а какие игнорировать как служебные, бинарные или сгенерированные.

РАЗРЕШЕНО:
- Читать файлы внутри `M:\GitHub\Shelter_2D`.
- Искать по Markdown, TXT, инструкциям, документации, проектным файлам и конфигурациям.
- Изменить только `БЛОК 2` файла `M:\GitHub\Shelter_2D\Shelter_2D\doc\EXTERNAL_AGENT_TASK.md`, чтобы записать отчёт.

ЗАПРЕЩЕНО:
- Не редактировать найденные документы.
- Не исправлять пути самостоятельно.
- Не использовать Git вообще: ни `git status`, ни `git diff`, ни `git grep`, ни любые другие Git-команды.
- Не менять `.git`, `.obsidian`, настройки IDE, Codex, OpenCode или VS Code.
- Не удалять, не перемещать и не переименовывать файлы.
- Не выходить за рабочую зону.
- Не менять `БЛОК 1`, `БЛОК 3` и раздел `ПРАВИЛА РАБОТЫ` в этом файле.

ЧТО ИСКАТЬ:
- Полный старый путь: `M:\GitHub\Shelter_2D\Shelter_2D_Game`
- Относительные и частичные упоминания старой папки: `Shelter_2D_Game`
- Упоминания solution/project/source путей, которые могут указывать на старое расположение.
- Отличать старый путь от нового: `M:\GitHub\Shelter_2D\Shelter_2D\Shelter_2D_Game`.

ПОВРЕЖДЁННАЯ КОДИРОВКА:
- Если видишь в путях, названиях файлов или тексте правил нечитаемую кириллицу, символы `�`, `????`, `РџСЂ...`, `Ð...` или другие признаки сломанной кодировки, не угадывай правильные имена файлов и не продолжай работу по повреждённым путям.
- Остановись, укажи проблему в `Problems`, верни статус `частично` или `неуспешно` и попроси корректный UTF-8 текст либо путь.

ЯЗЫК ОТЧЁТА:
- Итоговый отчёт всегда пиши на русском языке.
- Технические термины, имена файлов, команды и ошибки можно оставлять на английском, но поясняй по-русски.

ФОРМАТ ОТЧЁТА В БЛОК 2:

```text
AGENT EXTENDED REPORT

Agent:
- <модель / среда / внешний через чат пользователя>

Status:
- успешно / частично / неуспешно

Task:
- TASK EXT 012 Shelter_2D — read-only анализ старых путей после переноса игры

Rules read:
- M:\GitHub\Shelter_2D\Shelter_2D\doc\EXTERNAL_AGENT_TASK.md

Mode:
- read-only для всего репозитория; edit только для БЛОК 2 файла EXTERNAL_AGENT_TASK.md

Scope:
- чтение: M:\GitHub\Shelter_2D
- запись отчёта: M:\GitHub\Shelter_2D\Shelter_2D\doc\EXTERNAL_AGENT_TASK.md, только БЛОК 2

1. ДОКУМЕНТАЦИЯ / ИНСТРУКЦИИ, КОТОРЫЕ НУЖНО ПРАВИТЬ
- файл и номер строки;
- текущий фрагмент;
- почему нужно менять;
- точная замена.

2. ИСТОРИЧЕСКИЕ ОТЧЁТЫ / АРХИВНЫЕ УПОМИНАНИЯ
- файл и номер строки;
- текущий фрагмент;
- рекомендация: оставить или менять;
- пояснение.

3. ПРОЕКТНЫЕ / СЛУЖЕБНЫЕ ФАЙЛЫ
- файл и номер строки, если это текстовый файл;
- текущий фрагмент;
- нужно ли менять;
- риск, если менять.

4. БИНАРНЫЕ / СГЕНЕРИРОВАННЫЕ АРТЕФАКТЫ
- найденные категории или файлы;
- рекомендация: игнорировать / не трогать.

5. ИТОГОВЫЙ СПИСОК РЕКОМЕНДУЕМЫХ ПРАВОК
- файл;
- строка;
- старое значение;
- новое значение.

Checks:
- Git не использовался.
- Найденные документы не редактировались.
- Изменён только БЛОК 2 файла EXTERNAL_AGENT_TASK.md.
- Отчёт на русском языке.

Problems:
- <проблемы или нет>

For main Codex:
- <что основному Codex нужно проверить или исправить>
```

ЗАВЕРШЕНИЕ:
- Запиши полный отчёт в `БЛОК 2` этого файла.
- В чат верни короткое резюме и укажи, что полный отчёт записан в `M:\GitHub\Shelter_2D\Shelter_2D\doc\EXTERNAL_AGENT_TASK.md`.
- Если записать отчёт в `БЛОК 2` невозможно, верни полный отчёт в чат и явно укажи причину.

ФИНАЛЬНЫЙ МАРКЕР В ЧАТ:
- `РАБОТА ЗАВЕРШЕНА`
- `Итог: успешно / частично / неуспешно`
- `Контроль возвращён основному Codex.`

---
# БЛОК 2 — РАСШИРЕННЫЙ ОТЧЁТ ВНЕШНЕГО СУБАГЕНТА

```text
Ожидается отчёт внешнего агента по TASK EXT 012.
```

---

# БЛОК 3 — КОРОТКОЕ СООБЩЕНИЕ ДЛЯ ЧАТА ВНЕШНЕМУ СУБАГЕНТУ

Основной агент даёт пользователю короткое сообщение для вставки в чат внешнему или локальному агенту.

Шаблон:

```text
Прочитай задачу внешнего субагента:

M:\GitHub\Shelter_2D\Shelter_2D\doc\EXTERNAL_AGENT_TASK.md

Эта задача подготовлена основным Codex.
Ты работаешь как управляемый внешний или локальный субагент через чат пользователя.
В начале работы и в отчёте укажи конкретного исполнителя: модель / среда / маршрут, например `GigaChat / VS Code / внешний через чат пользователя`.
КОНТРОЛЬ МАРШРУТА АГЕНТОВ:
- Внутренние агенты Codex: `explorer`, `worker`, `default` и другие Codex-native роли. Это не внешние агенты.
- Локальные агенты: запускаются из Codex, но не являются внутренними агентами Codex; пример `simple_local / qwen3-coder:30b / локальный`.
- Внешние агенты: запускаются из других сред, не из Codex; задачу им передаёт пользователь через отдельный чат.
- Если требуется внешний агент, основной Codex не запускает `explorer`, `worker`, `default` или `simple_local`, а готовит внешний маршрут через `EXTERNAL_AGENT_TASK.md`.
Не принимай самостоятельные расширения задачи вне БЛОК 1.
Выполни активную задачу из БЛОК 1.
Соблюдай режим работы из задачи: edit только в указанном scope.
Читай и меняй только файлы, явно перечисленные в scope.
Всё, что не перечислено в scope, запрещено.
Git не используй вообще: ни для чтения, ни для проверки, ни для изменения.
Не трогай `.git`, `.obsidian`, Codex config, OpenCode config и VS Code config.
Важно: не бери активную задачу из `Задача_агенту.md` как источник требований. Источник задачи только БЛОК 1 файла `EXTERNAL_AGENT_TASK.md`.
Важно: `EXTERNAL_AGENT_TASK.md` разрешено менять только в `БЛОК 2 — РАСШИРЕННЫЙ ОТЧЁТ ВНЕШНЕГО СУБАГЕНТА`. `БЛОК 1`, `БЛОК 3` и остальные разделы этого файла не меняй.
Сохраняй Markdown как обычный UTF-8 текст с настоящими переносами строк. Не записывай файл одной строкой, не вставляй буквальные `\n`, не экранируй Windows-пути двойными слэшами.
Жёсткая проверка перед статусом `успешно`: перечитай каждый созданный или изменённый Markdown-файл и проверь, что он не одной строкой, переносы строк настоящие, нет буквальных escape-переносов вместо строк, Windows-пути не превращены в JSON-строки с двойными слэшами, кириллица читается нормально.
Если видишь в путях, названиях файлов или тексте правил нечитаемую кириллицу, символы `�`, `????`, `РџСЂ...`, `Ð...` или другие признаки сломанной кодировки, не угадывай правильные имена файлов и не продолжай работу по повреждённым путям. Остановись, укажи проблему кодировки в `Problems`, верни `Status: частично` или `неуспешно` и попроси корректный UTF-8 текст либо путь.
Если любой форматный пункт нарушен, не пиши `Status: успешно` и не пиши `РАБОТА ЗАВЕРШЕНА`. Верни `Status: частично` или `неуспешно`, укажи повреждённый файл и причину.
Не очищай БЛОК 1 самостоятельно: после проверки это делает основной Codex.
Не удерживай сессию пустыми рассуждениями или псевдо-вызовами инструментов.
Если нужный инструмент недоступен, сразу напиши это в отчёте и остановись.

По завершении:
1. Обязательно верни блок `USER SUMMARY` — коротко для пользователя.
2. Обязательно запиши блок `AGENT EXTENDED REPORT` в БЛОК 2 файла задачи.
3. В поле `Agent` укажи конкретно: модель / среда / маршрут через чат пользователя.
4. В поле `Status` укажи итог: `успешно`, `частично` или `неуспешно`.
5. Если запись в `БЛОК 2` невозможна, верни расширенный отчёт в чат и честно укажи недоступный инструмент или permission.
6. Последними строками `USER SUMMARY` напиши один из маркеров:

```text
РАБОТА ЗАВЕРШЕНА
Итог: успешно
Контроль возвращён основному Codex.
```

или

```text
РАБОТА НЕ ЗАВЕРШЕНА
Итог: частично / неуспешно
Контроль возвращён основному Codex.
```
```

---

# ПРАВИЛА РАБОТЫ

Основной агент:

- пишет активную задачу в `БЛОК 1`;
- включает в `БЛОК 1` обязательный блок `КОНТРОЛЬ МАРШРУТА АГЕНТОВ`;
- даёт пользователю короткое сообщение из `БЛОК 3`;
- не выполняет задачу сам как внешний агент;
- не пишет `AGENT EXTENDED REPORT` от имени внешнего агента;
- не создаёт и не ждёт внешний чат внутри Codex, если внешний агент работает через чат пользователя;
- если `БЛОК 1` пустой, не имитирует неуспешный внешний запуск, а сначала записывает задачу в `БЛОК 1` или сообщает пользователю, что внешнему агенту нечего выполнять;
- после выполнения читает короткий отчёт пользователя и расширенный отчёт из `БЛОК 2` или из чата;
- возвращает в основной чат краткую выдержку как `SUBAGENT REPORT`;
- в `SUBAGENT REPORT` всегда указывает конкретного агента и то, что обращение шло через чат пользователя;
- проверяет результат и принимает финальное решение сам.

Внешний или локальный агент через чат пользователя:

- читает этот файл;
- выполняет только `БЛОК 1`;
- не выходит за scope;
- работает локально на ПК и не использует Git;
- даёт пользователю короткий отчёт;
- пишет расширенный отчёт в `БЛОК 2` или возвращает его текстом пользователю;
- не очищает `БЛОК 1` самостоятельно, если это не разрешено отдельной задачей;
- завершает ответ явным маркером `РАБОТА ЗАВЕРШЕНА` или `РАБОТА НЕ ЗАВЕРШЕНА`;
- сохраняет Markdown обычным UTF-8 текстом с настоящими переносами строк;
- перед статусом `успешно` сам проверяет, что созданные или изменённые Markdown-файлы не стали одной строкой, не содержат буквальные escape-переносы вместо строк и не имеют экранированных Windows-путей.

Если задача внешнего или локального агента через чат пользователя успешно завершена и отчёт принят, основной агент сначала проверяет, что расширенный отчёт записан в `БЛОК 2`, а затем очищает `БЛОК 1` до состояния "Активной задачи для внешнего субагента нет".

Если задача основного агента успешно выполнена, основной агент сначала записывает итоговый отчёт в `БЛОК 2 — ПОСЛЕДНИЙ ОТЧЁТ АГЕНТА` файла `Задача_агенту.md`, а затем очищает `БЛОК 1 — ТЕКУЩАЯ ЗАДАЧА` в `Задача_агенту.md`.

Если задача выполнена частично, неуспешно или требует решения пользователя, `БЛОК 1` не очищается, а причина указывается в `PROBLEMS`.
