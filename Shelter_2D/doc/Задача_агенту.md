# Задача_агенту

Это рабочий документ для постановки задач агенту в проекте VR_Project.

## Связи Obsidian

- [[Старт]]
- [[История_задач]]
- [[Задача_внешнему_субагенту]]
- [[Список_правил]]

Документ хранит:

- большой блок текущей задачи;
- последний отчёт агента;
- проверку результата / status;
- карту чтения проектной документации;
- правило большого и малого блока;
- правило задач от второго агента;
- правило использования субагентов;
- компактный формат отчёта агента.

---

# БЛОК 1 — ТЕКУЩАЯ ЗАДАЧА

# Задача агенту

TASK 002 — Проверить `Старт.md`, добавить правило чтения GitVerse через SSH для ChatGPT и подготовить отчёт для review

Статус:  
Новая активная задача для агента.

Тип задачи:  
Документация с ограниченными правками.

Главная цель:  
Агент должен самостоятельно прочитать актуальное состояние Git через локальный SSH-доступ пользователя, проверить текущий `Старт.md`, добавить правило о безопасном чтении GitVerse/SSH для ChatGPT и включить в отчёт всю информацию, которая нужна ChatGPT для review.

## 1. Важный контекст

У ChatGPT нет прямого SSH-доступа к приватному GitVerse и нет доступа к локальному диску пользователя.

Поэтому агент обязан сам получить данные локально и включить в отчёт для ChatGPT:

- текущий Git root;
    
- текущий commit;
    
- актуальный путь к `Старт.md`;
    
- содержимое или diff `Старт.md`;
    
- что именно было изменено;
    
- какие Git-команды только на чтение выполнялись.
    

Запрещено передавать ChatGPT приватные SSH-ключи, пароли, токены, содержимое `~/.ssh/id_*` и секретные конфиги.

## 2. Что прочитать перед началом

Агент обязан прочитать актуальные правила, если файлы существуют:

- `M:\GITS_VERSE\VR_Project\VR_Projekt\Doc\Старт.md`
    
- `M:\GITS_VERSE\VR_Project\VR_Projekt\Doc\Задача_агенту.md`
    
- `M:\GITS_VERSE\VR_Project\VR_Projekt\Doc\Правила_Git_и_Review.md`
    
- `M:\GITS_VERSE\VR_Project\VR_Projekt\Doc\Правила_создания_документов.md`
    

Также проверить, существуют ли документы в перенесённой структуре:

- `M:\GITS_VERSE\VR_Project\VR_Projekt\VR\Doc\Старт.md`
    
- `M:\GITS_VERSE\VR_Project\VR_Projekt\VR\Doc\Задача_агенту.md`
    
- `M:\GITS_VERSE\VR_Project\VR_Projekt\VR\Doc\Правила_Git_и_Review.md`
    
- `M:\GITS_VERSE\VR_Project\VR_Projekt\VR\Doc\Правила_создания_документов.md`
    

Важно:  
Не исправлять всю структуру проекта в этой задаче. Сейчас задача только про `Старт.md`, правило SSH-чтения для ChatGPT и отчёт.

## 3. Git-команды только на чтение

Разрешено выполнить только команды чтения:

- `git rev-parse --show-toplevel`
    
- `git status --short`
    
- `git log --oneline -10`
    
- `git show --stat --name-status HEAD`
    
- `git ls-tree -r --name-only HEAD | grep -E "Старт.md|Задача_агенту.md|Правила_Git_и_Review.md|Правила_создания_документов.md"`
    
- `git show HEAD:<path_to_start>`
    
- `git diff -- <path_to_start>`
    

Если `grep` не работает, использовать альтернативу Git Bash / PowerShell, но только для чтения.

Запрещено:

- `git commit`
    
- `git push`
    
- `git pull`
    
- `git merge`
    
- `git rebase`
    
- `git reset`
    
- `git clean`
    
- `git checkout`
    
- `git switch`
    
- создание веток
    
- удаление веток
    
- изменение remote
    
- изменение Git-конфига
    

## 4. Что нужно проверить в `Старт.md`

Агент должен найти актуальный `Старт.md`.

Проверить:

- где реально находится `Старт.md`;
    
- нет ли дублирования текста;
    
- нет ли битой Markdown-разметки;
    
- нет ли строк вида `text` без нормального Markdown-контекста;
    
- не повреждена ли кириллица;
    
- указана ли актуальная ссылка репозитория: `git@gitverse.ru:MLPGM/VR_Project.git`;
    
- указаны ли актуальные пути проекта;
    
- корректно ли описана связь `Doc` и Obsidian-хранилища `VR`;
    
- не утверждается ли, что ChatGPT может напрямую читать приватный GitVerse через SSH.
    

## 5. Разрешённые правки

Разрешено изменить только актуальный файл `Старт.md`, который агент найдёт в текущей принятой структуре.

Если `Старт.md` найден в двух местах, агент не должен молча править оба.

Нужно:

1. указать в отчёте оба найденных пути;
    
2. определить, какой файл сейчас считается актуальным по текущей структуре;
    
3. изменить только актуальный `Старт.md`;
    
4. второй файл не трогать без отдельной команды.
    

Разрешённая правка:  
Добавить в `Старт.md` раздел `Правило чтения GitVerse / SSH для ChatGPT`.

Текст раздела должен закреплять:

- ChatGPT видит SSH-адрес, commit hash, вывод команд, diff, отчёты агентов и загруженные файлы;
    
- ChatGPT не имеет прямого доступа к приватному GitVerse по SSH;
    
- ChatGPT не имеет доступа к локальному диску пользователя;
    
- пользователь или локальный агент должны передавать ChatGPT безопасные данные;
    
- запрещено передавать ChatGPT приватные SSH-ключи, пароли, токены и секретные конфиги;
    
- локальный агент может читать Git через SSH только если задача это разрешает;
    
- локальный агент должен включать в отчёт Git-команды чтения, commit hash, branch/ref, список прочитанных файлов и нужные diff/show фрагменты;
    
- ChatGPT обязан честно различать: видит адрес репозитория, видит commit hash, видит вывод Git-команды, видит содержимое файла, имеет прямой SSH-доступ.
    

Если в `Старт.md` уже есть похожий раздел, не дублировать его, а аккуратно обновить.

Если в `Старт.md` есть явная битая Markdown-разметка вокруг карты проекта, разрешено исправить только эту локальную разметку, не переписывая весь документ.

## 6. Запрещённые правки

Запрещено:

- менять файлы Unreal Engine;
    
- менять `VR_Horror.uproject`;
    
- менять `Source`;
    
- менять `Content`;
    
- менять `Config`;
    
- менять Blueprint-активы;
    
- менять C++ файлы;
    
- менять `.obsidian`;
    
- переносить документы между `Doc` и `VR\Doc`;
    
- удалять документы;
    
- массово переименовывать документы;
    
- исправлять всю структуру проекта;
    
- чинить Git-историю;
    
- делать commit;
    
- делать push.
    

Если агент видит, что структура документов спорная или неправильная, он должен описать это в `PROBLEMS` и предложить следующую задачу, но не исправлять всё в этой задаче.

## 7. Что обязательно включить в отчёт для ChatGPT

В отчёт обязательно включить:

1. Git root:  
    результат `git rev-parse --show-toplevel`.
    
2. Git status:  
    результат `git status --short`.
    
3. Последние commits:  
    результат `git log --oneline -10`.
    
4. Текущий commit:  
    short SHA и full SHA, если доступен.
    
5. Где найден `Старт.md`:  
    полный путь или Git path.
    
6. Полный текст `Старт.md` до правки или результат `git show HEAD:<path_to_start>`.
    

Если полный текст слишком большой, включить:

- первые строки;
    
- проблемный фрагмент;
    
- фрагмент карты проекта;
    
- фрагмент нового правила;
    
- полный `git diff -- <path_to_start>` после правки.
    

7. Полный diff после правки:  
    результат `git diff -- <path_to_start>`.
    
8. Список команд Git, которые выполнялись:  
    только команды чтения.
    
9. Подтверждение:  
    секреты, SSH-ключи, токены и пароли не выводились и не передавались.
    

## 8. Кодировка

Все Markdown-файлы читать и писать как UTF-8.

Русский текст нельзя повреждать.

Если используется Python:

- читать через `encoding="utf-8"`;
    
- писать через `encoding="utf-8"`.
    

Если используется PowerShell:

- не использовать `Set-Content` / `Out-File` без явного указания UTF-8;
    
- перед перезаписью проверить, что кириллица читается нормально.
    

Если после чтения текст выглядит повреждённым, агент обязан остановиться и написать об этом в отчёте.

## 9. Формат отчёта

Вернуть отчёт в чат в формате:

AGENT REPORT

AGENT ROLE:

- указать агент и роль, например: Agent-1 / Исполнитель или Agent-2 / Review
    

RULES READ:

- какие документы прочитаны
    
- какие документы не найдены
    

GITVERSE STATUS:

- repository
    
- git root
    
- branch/ref
    
- HEAD commit
    
- commit message
    
- Git-команды только на чтение, которые выполнялись
    

START FILE STATUS:

- где найден `Старт.md`
    
- есть ли дубли
    
- есть ли битая Markdown-разметка
    
- есть ли проблемы с кириллицей
    

DONE:

- что сделано
    

CHANGED FILES:

- какие файлы изменены
    
- если изменён только `Старт.md`, так и написать
    

DIFF / SUMMARY:

- кратко что изменилось
    
- вставить полный `git diff -- <path_to_start>`
    

DATA FOR CHATGPT REVIEW:

- вставить нужные фрагменты `Старт.md`
    
- вставить новый раздел про GitVerse / SSH
    
- вставить проблемные места, если они есть
    

ENCODING CHECK:

- подтверждение, что кириллица читается нормально
    
- подтверждение UTF-8
    

BUILD:

- написать: сборка Unreal Engine не требуется, менялся только Markdown
    

GIT:

- написать: субагенты работали локально на ПК и Git не использовали; основной агент Git-состояние не менял
    
- commit / push / pull / merge / reset / checkout / switch не выполнялись
    

SECRETS CHECK:

- SSH-ключи, токены, пароли и секретные конфиги не выводились и не передавались
    

PROBLEMS:

- реальные проблемы и риски
    
- если структура `Doc` / `VR\Doc` спорная, описать, но не исправлять
    

QUESTIONS:

- вопросы к пользователю, если есть
    

RECOMMENDED NEXT TASK:

- предложить следующий безопасный шаг, если нужно отдельно разбирать структуру документов
    

CONCLUSION:

- итог задачи
    

## 10. После выполнения

После выполнения не делать commit и не делать push.

`Задача_агенту.md` не очищать автоматически, пока пользователь не подтвердит, что отчёт принят, потому что ChatGPT должен сначала сделать review по отчёту.


---

# БЛОК 2 — ПОСЛЕДНИЙ ОТЧЁТ АГЕНТА

DONE:
- Обновлены стартовые документы проекта VR_Project.
- Закреплены актуальные пути проекта, GitVerse repository и правило Obsidian Vault `VR`.
- Обновлены правила Git / Review под GitVerse.
- Обновлены правила создания документов с разделением `Doc` и `VR`.
- Создан документ `Правила_использования_субагентов.md`.
- Закреплён приоритет облачного `qwen3-coder:480b-cloud`, если он доступен в текущей среде.
- Добавлено правило: субагенты читают минимальный набор правил, а основной агент жёстко контролирует scope и финальное решение.
- Добавлено разделение отчётов субагентов на промежуточные лаконичные `SUBAGENT UPDATE` и итоговый `SUBAGENT REPORT`.
- Активная задача в `БЛОК 1` очищена после выполнения TASK 001.

CHANGED:
- `M:\GITS_VERSE\VR_Project\VR_Projekt\VR\Doc\Старт.md`
- `M:\GITS_VERSE\VR_Project\VR_Projekt\VR\Doc\Правила_Git_и_Review.md`
- `M:\GITS_VERSE\VR_Project\VR_Projekt\VR\Doc\Правила_создания_документов.md`
- `M:\GITS_VERSE\VR_Project\VR_Projekt\VR\Doc\Правила_использования_субагентов.md`
- `M:\GITS_VERSE\VR_Project\VR_Projekt\VR\Doc\Задача_агенту.md`

UNEXPECTED CHANGES:
- Нет.

UNTRACKED:
- Не проверялось Git-командами; Git не использовался в задаче.

CHECKS:
- Проверено, что стартовые документы и документ правил субагентов существуют.
- Проверено, что в стартовых правилах нет старых названий, путей и ссылок от других проектов.
- Проверено наличие актуального GitVerse repository `git@gitverse.ru:MLPGM/VR_Project.git`.
- Проверено наличие актуальных путей `M:\GITS_VERSE\VR_Project\VR_Projekt`, `Doc`, `VR`, `VR_Horror\VR_Horror.uproject`.
- Проверено, что кириллица читается нормально при UTF-8.
- Сборка Unreal Engine не требуется, менялись только Markdown-документы.

GIT:
- Субагенты работали локально на ПК и Git не использовали.
- Основной агент Git-состояние не менял.
- Git-команды чтения не выполнялись.
- Commit не создавался агентом.
- Push не выполнялся агентом.
- Pull не выполнялся агентом.
- Ветки не создавались и не переключались агентом.
- Git-история не изменялась агентом.

PROBLEMS:
- Нет.

NEXT:
- Ожидается новая задача от пользователя.

---

# БЛОК 3 — REVIEW / STATUS

## НАЧАЛО REVIEW

Статус:
- Принято: `M:\GITS_VERSE\VR_Project\VR_Projekt\VR\Doc\Старт.md` является главным документом для первичного чтения.
- Принято: `M:\GITS_VERSE\VR_Project\VR_Projekt\VR\Doc\Задача_агенту.md` хранит активную задачу, последний отчёт и служебные правила агентского цикла.
- Принято: `M:\GITS_VERSE\VR_Project\VR_Projekt\VR\Doc` — строгая папка правил и задач агента.
- Принято: `M:\GITS_VERSE\VR_Project\VR_Projekt\VR` — Obsidian Vault проекта.
- Принято: пользовательские Markdown-файлы называются на русском языке, если нет технической причины для другого имени.
- Принято: второй агент может готовить задачи для выполнения, но прямое сообщение пользователя имеет приоритет.
- Принято: правила использования субагентов хранятся в `M:\GITS_VERSE\VR_Project\VR_Projekt\VR\Doc\Правила_использования_субагентов.md`.

Baseline:
- Локальное состояние стартовых документов VR_Project после выполнения TASK 001.

Что изменено:
- Стартовые правила адаптированы под VR_Project.
- Закреплены актуальные пути проекта и GitVerse repository `git@gitverse.ru:MLPGM/VR_Project.git`.
- Закреплено разделение `Doc` и Obsidian Vault `VR`.
- Добавлены правила использования облачных и локальных субагентов.
- Добавлена обязательная отчётность субагентов в чат пользователя.

Known limitations:
- Технические файлы Git, Unreal Engine и Obsidian могут иметь служебные англоязычные имена.

## КОНЕЦ REVIEW

---

# ПРАВИЛО БОЛЬШОГО И МАЛОГО БЛОКА

Задачи могут приходить от пользователя напрямую или через второго агента, который готовит грамотную постановку задачи.

Задача второго агента не отменяет правила проекта и прямые сообщения пользователя.

## Большой блок

Большой блок:

- хранится в `M:\GITS_VERSE\VR_Project\VR_Projekt\VR\Doc\Задача_агенту.md`;
- остаётся основной задачей в документе;
- содержит цель, контекст, scope, разрешённые файлы, запреты и проверки;
- не переписывается заново только из-за поздних уточнений.

## Малый блок

Малый блок:

- отправляется агенту в чат;
- содержит команду прочитать актуальную задачу из `M:\GITS_VERSE\VR_Project\VR_Projekt\VR\Doc\Задача_агенту.md`;
- может содержать список дополнительных документов для чтения;
- может содержать уточнения, появившиеся после подготовки большого блока;
- может содержать критичные напоминания;
- дополняет большой блок, но не заменяет его.

Малый блок может быть написан пользователем или подготовлен вторым агентом.

Если уточнение появилось после подготовки большого блока и до отчёта агента, оно добавляется в малый блок без переписывания большого блока.

Если малый блок противоречит большому блоку, агент обязан остановиться и указать конфликт в отчёте.

Если задача второго агента противоречит прямому сообщению пользователя, приоритет имеет прямое сообщение пользователя.

---

# КОМПАКТНЫЙ ОТЧЁТ АГЕНТА

Последний отчёт агента хранится в `M:\GITS_VERSE\VR_Project\VR_Projekt\VR\Doc\Задача_агенту.md` в блоке `БЛОК 2 — ПОСЛЕДНИЙ ОТЧЁТ АГЕНТА`, ниже активной задачи.

Перед записью отчёта агент сверяет фактическое состояние файлов через безопасные Git-проверки чтения, если Git использовался в задаче.

Если `git status`, `git diff --name-only`, `git ls-files --others --exclude-standard` или другая безопасная Git-проверка чтения показывает файл как изменённый, добавленный, удалённый или untracked, этот файл обязательно указывается в отчёте.

Файлы разделяются так:

- `CHANGED` — намеренно изменённые файлы в рамках задачи;
- `UNEXPECTED CHANGES` — побочные, автоматические или не входившие в задачу изменения;
- `UNTRACKED` — новые файлы, которые ещё не отслеживаются Git.

Побочные изменения нельзя скрывать. Если, например, `.obsidian\workspace.json` изменился автоматически из-за Obsidian, но задача не разрешала менять его намеренно, файл указывается в `UNEXPECTED CHANGES`. Если файл уже отслеживается Git и это создаёт риск, риск указывается в `PROBLEMS`.

Формат отчёта:

```text
DONE:
- что сделано

CHANGED:
- намеренно изменённые файлы

UNEXPECTED CHANGES:
- побочные или автоматические изменения

UNTRACKED:
- новые неотслеживаемые файлы

CHECKS:
- что проверено

GIT:
- commit не создавался агентом
- push не выполнялся агентом
- pull не выполнялся агентом
- ветки не создавались и не переключались агентом
- Git-история не изменялась агентом

PROBLEMS:
- реальные проблемы или риски

NEXT:
- следующий шаг
```

## Правило очистки активной задачи после отчёта

После успешного выполнения задачи агент обязан очистить `БЛОК 1 — ТЕКУЩАЯ ЗАДАЧА`.

После очистки `БЛОК 1` должен содержать только:

```text
БЛОК 1 — ТЕКУЩАЯ ЗАДАЧА

Активной задачи нет.
Предыдущая задача выполнена.
Ожидается новая задача от пользователя.
```

Если задача не выполнена, есть критичная ошибка или требуется решение пользователя, `БЛОК 1` не очищать. В этом случае агент обязан явно указать причину в `PROBLEMS`.

Очистка `БЛОКА 1` выполняется только после того, как отчёт записан в `БЛОК 2 — ПОСЛЕДНИЙ ОТЧЁТ АГЕНТА`.

---

# КАРТА ЧТЕНИЯ

Перед началом любой задачи читать:

```text
M:\GITS_VERSE\VR_Project\VR_Projekt\VR\Doc\Старт.md
M:\GITS_VERSE\VR_Project\VR_Projekt\VR\Doc\Задача_агенту.md
```

Тематическое чтение:

```text
Создание документов -> M:\GITS_VERSE\VR_Project\VR_Projekt\VR\Doc\Правила_создания_документов.md
Git / GitVerse / проверка результата -> M:\GITS_VERSE\VR_Project\VR_Projekt\VR\Doc\Правила_Git_и_Review.md
Субагенты / делегирование / облачные и локальные агенты -> M:\GITS_VERSE\VR_Project\VR_Projekt\VR\Doc\Правила_использования_субагентов.md
Активная задача и последний отчёт -> M:\GITS_VERSE\VR_Project\VR_Projekt\VR\Doc\Задача_агенту.md
Стартовая карта проекта -> M:\GITS_VERSE\VR_Project\VR_Projekt\VR\Doc\Старт.md
Obsidian Vault проекта -> M:\GITS_VERSE\VR_Project\VR_Projekt\VR
Unreal-проект -> M:\GITS_VERSE\VR_Project\VR_Projekt\VR_Horror\VR_Horror.uproject
```

Агент читает только документы, нужные для текущей задачи.

Если нужного документа нет, указать это в отчёте.
