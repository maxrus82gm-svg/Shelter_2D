# Задача_агенту

Рабочий документ активной задачи проекта `Shelter_2D`.

## Связи Obsidian

- [[Список_правил]]
- [[Список_форм_отчётов]]

# БЛОК 1 — ТЕКУЩАЯ ЗАДАЧА
Активной задачи нет.
Предыдущая задача выполнена.
Ожидается новая задача от пользователя.

# БЛОК 2 — ПОСЛЕДНИЙ ОТЧЁТ АГЕНТА

DONE:
- Прочитаны `Старт.md`, `Задача_агенту.md`, `Список_правил.md`, тематические правила Git/проверок, правила создания документов и формы отчётов.
- Исправлено правило review/commit: агент-исполнитель не пишет commit-статус, не указывает итоговый commit результата и не определяет актуальную верхушку репозитория для ChatGPT-review.
- Закреплено, что агент может писать только `commit не создавался`, `push не выполнялся`, `Git-история не изменялась` и список выполненных read-only Git-проверок.
- Закреплено, что `git log -1`, если он используется агентом по разрешённой задаче, считается только baseline до работы или до пользовательского commit.
- Закреплено, что актуальный последний commit для review определяет только ChatGPT после отчёта агента, самостоятельно проверяя репозиторий или GitHub.
- Обновлена форма agent-report: commit-статус не входит в отчёт агента.
- Обновлена форма review-report: commit-статус относится только к ChatGPT-review.
- `БЛОК 1` оставлен очищенным после успешной задачи.

CHANGED:
- `m:\GitHub\Shelter_2D\Shelter_2D\doc\Старт.md`
- `m:\GitHub\Shelter_2D\Shelter_2D\doc\Задача_агенту.md`
- `m:\GitHub\Shelter_2D\Shelter_2D\doc\Правила\Правила_Git_и_проверки.md`
- `m:\GitHub\Shelter_2D\Shelter_2D\doc\Формы_отчётов\AGENT_REPORT.md`
- `m:\GitHub\Shelter_2D\Shelter_2D\doc\Формы_отчётов\REVIEW_REPORT.md`

DELETED:
- нет

UNEXPECTED CHANGES:
- `m:\GitHub\Shelter_2D\Shelter_2D\.obsidian\graph.json` появился в diff вне scope; агент его не редактировал.
- `m:\GitHub\Shelter_2D\Shelter_2D\.obsidian\workspace.json` появился в diff вне scope; агент его не редактировал.

UNTRACKED:
- нет

CHECKS:
- `git status --short --untracked-files=all`
- `git diff --name-only`
- `git diff --check`
- `rg` по рабочим Markdown-документам на старые и новые формулировки review/commit.
- Проверено, что `Старт.md` содержит разделение обязанностей агента и ChatGPT-review.
- Проверено, что `Задача_агенту.md` содержит разделение обязанностей агента и ChatGPT-review.
- Проверено, что `Правила\Правила_Git_и_проверки.md` содержит полный порядок review с самостоятельной проверкой commit только ChatGPT.
- Проверено, что формы отчётов не смешивают agent-report и review-report.

GIT:
- commit не создавался
- push не выполнялся
- Git-история не изменялась

PROBLEMS:
- Блокирующих проблем нет.
- В diff появились изменения `.obsidian`; они вне scope и не редактировались агентом.
- `git diff --check` выполнен без ошибок; есть предупреждения Git о будущей нормализации LF/CRLF.

NEXT:
- Следующий ChatGPT-review после отчёта агента самостоятельно определяет актуальный commit и только потом даёт пользователю вывод.

# БЛОК 3 — REVIEW / STATUS

## Большой блок

Большой блок хранится в:

```text
m:\GitHub\Shelter_2D\Shelter_2D\doc\Задача_агенту.md
```

Он содержит цель, контекст, scope, разрешённые файлы, запреты, проверки и формат отчёта.

## Малый блок

Малый блок отправляется в чат и дополняет большой блок поздними уточнениями.

Если малый блок противоречит большому, исполнитель останавливается и пишет конфликт в `PROBLEMS`.

Прямое сообщение пользователя имеет приоритет над текстом в рабочем документе.

## Правило review после отчёта агента

После каждого отчёта агента ChatGPT обязан сразу проверить результат в репозитории или доступных файлах и только потом давать пользователю итоговый вывод.

Это правило относится к ChatGPT-review после отчёта агента.

Агент-исполнитель в своём итоговом отчёте:

- не пишет блок `СТАТУС КОММИТА`;
- не указывает `Последний проверенный commit` как итоговый commit результата;
- не определяет актуальную верхушку репозитория для ChatGPT-review;
- может писать только `commit не создавался`, `push не выполнялся`, `Git-история не изменялась` и список выполненных read-only Git-проверок.

Если агент использует `git log -1`, этот результат считается только baseline до работы или до пользовательского commit, а не итоговым commit результата.

Актуальный последний commit для review определяет только ChatGPT после отчёта агента, самостоятельно проверяя репозиторий или GitHub.

Порядок обязателен:

1. Прочитать отчёт агента.
2. Проверить результат в репозитории или доступных файлах.
3. Самостоятельно определить актуальный последний commit, который проверяется.
4. Указать этот commit в начале review-ответа ChatGPT.
5. Сверить `DONE`, `CHANGED`, `DELETED`, `UNEXPECTED CHANGES`, `UNTRACKED`, `CHECKS`, `GIT`, `PROBLEMS`, `NEXT` с фактическим состоянием.
6. Проверить, что `БЛОК 1 — ТЕКУЩАЯ ЗАДАЧА` очищен после успешной задачи.
7. Проверить, что отчёт записан в `БЛОК 2 — ПОСЛЕДНИЙ ОТЧЁТ АГЕНТА`.
8. Проверить, что запрещённые файлы и папки не менялись.
9. Только после проверки дать пользователю вывод.

Формат начала review-ответа:

```text
СТАТУС КОММИТА
Последний проверенный commit: <номер или short sha>
Commit message: <message>
Repository: <owner/repo>
Branch: <branch>
Проверка: выполнена / частично выполнена / не выполнена
```

Если последний commit определить невозможно, ChatGPT начинает review так:

```text
СТАТУС КОММИТА
Последний проверенный commit: НЕ ПРОВЕРЕН
Причина: <почему не удалось проверить>
Проверка: частично выполнена
```

Если проверка невозможна, ChatGPT пишет:

```text
Проверка репозитория не выполнена: <причина>.
```

После этого ChatGPT даёт только предварительный вывод.

## Схема исполнения

```text
ChatGPT готовит большой и малый блок; пользователь передаёт последнюю версию малого блока Codex или другому выбранному исполнителю.
```

# ПРАВИЛО БОЛЬШОГО И МАЛОГО БЛОКА

Большой блок задаёт основной scope.

Последний малый блок уточняет задачу, но не расширяет запрещённые файлы и не отменяет Git-политику без прямого разрешения.

Если малый блок требует действия вне scope, исполнитель останавливается и фиксирует вопрос в `PROBLEMS`.

# КОМПАКТНЫЙ ОТЧЁТ АГЕНТА

Итоговый отчёт в чате и в `БЛОК 2` пишется в формате:

```text
DONE:
- что сделано

CHANGED:
- намеренно изменённые файлы

DELETED:
- удалённые файлы и папки

UNEXPECTED CHANGES:
- побочные или автоматические изменения

UNTRACKED:
- новые неотслеживаемые файлы

CHECKS:
- что проверено

GIT:
- commit не создавался
- push не выполнялся
- Git-история не изменялась

PROBLEMS:
- реальные проблемы или риски

NEXT:
- следующий шаг
```

# КАРТА ЧТЕНИЯ

Перед задачей исполнитель читает:

```text
m:\GitHub\Shelter_2D\Shelter_2D\doc\Старт.md
m:\GitHub\Shelter_2D\Shelter_2D\doc\Задача_агенту.md
```

Затем открывает тематические правила через:

```text
m:\GitHub\Shelter_2D\Shelter_2D\doc\Список_правил.md
```

Для форм отчётов используется:

```text
m:\GitHub\Shelter_2D\Shelter_2D\doc\Список_форм_отчётов.md
```
