# Задача_агенту

Это рабочий документ для постановки задач агенту в проекте `Shelter_2D`.

## Связи Obsidian

- [[Старт]]
- [[История_задач]]
- [[Задача_внешнему_субагенту]]
- [[Список_правил]]

Документ хранит:

- текущую задачу;
- последний отчёт агента;
- проверку результата / status;
- карту чтения проектной документации;
- правило большого и малого блока;
- правило задач от второго агента;
- правило использования субагентов;
- компактный формат отчёта агента.

---

# БЛОК 1 — ТЕКУЩАЯ ЗАДАЧА

# БЛОК 1 — ТЕКУЩАЯ ЗАДАЧА ВНЕШНЕМУ СУБАГЕНТУ

# TASK EXT 006 Shelter_2D — Синхронизировать manifest шаблона и правило unexpected `.obsidian`

Статус:  
Новая активная задача внешнему агенту.

Исполнитель:  
Внешний или локальный агент через чат пользователя.

Рекомендуемый маршрут:  
`GigaChat / VS Code / внешний через чат пользователя`  
или другой внешний агент, доступный пользователю.

Режим:  
`edit`

Git:  
Не использовать.

---

## 1. Главная цель

Нужно выполнить маленькую техническую правку после проверки `TASK 005`.

Есть два замечания:

1. В `MANIFEST_структуры.md` порядок чтения шаблона не полностью совпадает с `README_как_переносить.md`, `Старт.md` и `Список_правил.md`:  
    в manifest в начальном порядке чтения нет файла:
    

```text
04_Dry_run_пример_переноса.md
```

Хотя ниже этот файл уже указан как обязательный.

2. Нужно жёстче закрепить правило: если при GitHub / Git / review-проверке обнаружены изменения в `.obsidian`, `.vscode`, workspace/session/cache-файлах или других служебных файлах, которые не были явно разрешены scope задачи, их нельзя писать как `UNCHANGED`.  
    Их нужно писать в `UNEXPECTED CHANGES`.
    

---

## 2. Рабочий проект

Проект:

```text
Shelter_2D
```

Рабочая директория:

```text
M:\GitHub\Shelter_2D
```

Obsidian Vault:

```text
M:\GitHub\Shelter_2D\Shelter_2D
```

Папка правил:

```text
M:\GitHub\Shelter_2D\Shelter_2D\doc
```

Папка шаблона:

```text
M:\GitHub\Shelter_2D\Shelter_2D\doc\Шаблон_агентской_системы
```

---

## 3. Что прочитать

Перед началом прочитать только эти файлы:

```text
M:\GitHub\Shelter_2D\Shelter_2D\doc\Задача_внешнему_субагенту.md
M:\GitHub\Shelter_2D\Shelter_2D\doc\Шаблон_агентской_системы\README_как_переносить.md
M:\GitHub\Shelter_2D\Shelter_2D\doc\Шаблон_агентской_системы\MANIFEST_структуры.md
M:\GitHub\Shelter_2D\Shelter_2D\doc\Правила_Git_и_Review.md
M:\GitHub\Shelter_2D\Shelter_2D\doc\Шаблон_агентской_системы\Шаблоны_документов\Правила_Git_и_Review.md
```

Не читать весь Vault.  
Не читать весь `doc`.  
Не искать по всему проекту.  
Не открывать другие документы без необходимости.

---

## 4. Разрешено менять

Разрешено изменить только эти файлы:

```text
M:\GitHub\Shelter_2D\Shelter_2D\doc\Шаблон_агентской_системы\MANIFEST_структуры.md
M:\GitHub\Shelter_2D\Shelter_2D\doc\Правила_Git_и_Review.md
M:\GitHub\Shelter_2D\Shelter_2D\doc\Шаблон_агентской_системы\Шаблоны_документов\Правила_Git_и_Review.md
M:\GitHub\Shelter_2D\Shelter_2D\doc\Задача_внешнему_субагенту.md
```

Файл `Задача_внешнему_субагенту.md` разрешено менять только в разделе:

```text
# БЛОК 2 — РАСШИРЕННЫЙ ОТЧЁТ ВНЕШНЕГО СУБАГЕНТА
```

`БЛОК 1` файла `Задача_внешнему_субагенту.md` не очищать.  
Очистку `БЛОК 1` делает основной Codex после проверки результата.

---

## 5. Запрещено

Запрещено:

- использовать Git;
    
- выполнять `git status`;
    
- выполнять `git diff`;
    
- выполнять `git log`;
    
- выполнять `git show`;
    
- выполнять `git add`;
    
- выполнять `git commit`;
    
- выполнять `git push`;
    
- выполнять `git pull`;
    
- менять `.git`;
    
- менять `.obsidian`;
    
- менять `.vscode`;
    
- менять реальные конфиги Codex;
    
- менять реальные конфиги OpenCode;
    
- менять исходный код проекта;
    
- менять `Задача_агенту.md`;
    
- менять `Старт.md`;
    
- менять `Список_правил.md`;
    
- менять `Правила_создания_документов.md`;
    
- менять `Правила_использования_субагентов.md`;
    
- менять `Настройка_прав_субагентов.md`;
    
- создавать новые файлы;
    
- удалять файлы;
    
- переименовывать файлы;
    
- переносить файлы;
    
- использовать других субагентов.
    

---

## 6. Что именно сделать

### 6.1. Исправить `MANIFEST_структуры.md`

Файл:

```text
M:\GitHub\Shelter_2D\Shelter_2D\doc\Шаблон_агентской_системы\MANIFEST_структуры.md
```

В разделе, где написано:

```text
Агент не должен начинать перенос с частного шаблона. Сначала читаются:
```

сейчас порядок должен быть синхронизирован с README и рабочими правилами.

Нужно, чтобы список включал:

```text
1. README_как_переносить.md
2. MANIFEST_структуры.md
3. 00_Паспорт_проекта.md
4. 02_Карта_переменных.md
5. 01_Правило_подгонки_под_новый_проект.md
6. 03_Чеклист_проверки_переноса.md
7. 04_Dry_run_пример_переноса.md
```

Не удалять `04_Dry_run_пример_переноса.md` из списка обязательных файлов.  
Он уже должен оставаться в структуре шаблона.

---

### 6.2. Обновить рабочие Git / Review правила

Файл:

```text
M:\GitHub\Shelter_2D\Shelter_2D\doc\Правила_Git_и_Review.md
```

Добавить правило в подходящий раздел про review / отчёт / unexpected changes.

Смысл правила:

```text
Если во время GitHub / Git / review-проверки обнаружены изменения в служебных файлах проекта, которые не были явно разрешены scope задачи, эти файлы указываются в UNEXPECTED CHANGES.

К таким файлам относятся:
- .obsidian/*
- .vscode/*
- workspace-файлы
- session-файлы
- cache-файлы
- временные файлы IDE

Запрещено писать `.obsidian`, `.vscode` или другие служебные файлы как UNCHANGED, если проверенный diff показывает, что они изменялись.

Если агент не выполнял Git / GitHub / diff-проверку, он не должен утверждать, что `.obsidian` или `.vscode` не менялись. В этом случае нужно писать: "не проверялось".
```

Формулировку можно сделать аккуратной, но смысл не менять.

---

### 6.3. Обновить шаблонные Git / Review правила

Файл:

```text
M:\GitHub\Shelter_2D\Shelter_2D\doc\Шаблон_агентской_системы\Шаблоны_документов\Правила_Git_и_Review.md
```

Добавить такое же правило, но в универсальной форме.

Использовать placeholders, если нужны пути:

```text
{{PROJECT_NAME}}
{{PROJECT_ROOT}}
{{DOC_FOLDER}}
```

Не вставлять жёсткие пути `Shelter_2D`.

---

## 7. Проверка перед завершением

Перед завершением проверить:

- `MANIFEST_структуры.md` содержит `04_Dry_run_пример_переноса.md` в порядке чтения;
    
- `04_Dry_run_пример_переноса.md` остался в списке обязательных файлов;
    
- `Правила_Git_и_Review.md` содержит правило про `.obsidian` / `.vscode` / workspace / служебные файлы как `UNEXPECTED CHANGES`;
    
- шаблонный `Правила_Git_и_Review.md` содержит такое же универсальное правило;
    
- `Задача_агенту.md` не менялся;
    
- `Старт.md` не менялся;
    
- `Список_правил.md` не менялся;
    
- `Правила_создания_документов.md` не менялся;
    
- `.obsidian` не менялся;
    
- `.git` не менялся;
    
- новые файлы не создавались;
    
- Git не использовался;
    
- Markdown сохранён нормальным UTF-8;
    
- нет буквальных `\n` вместо переносов;
    
- кириллица читается нормально.
    

---

## 8. Расширенный отчёт

После выполнения записать расширенный отчёт в:

```text
M:\GitHub\Shelter_2D\Shelter_2D\doc\Задача_внешнему_субагенту.md
```

только в раздел:

```text
# БЛОК 2 — РАСШИРЕННЫЙ ОТЧЁТ ВНЕШНЕГО СУБАГЕНТА
```

Формат:

```text
AGENT EXTENDED REPORT

Agent:
- указать модель / среду / маршрут через чат пользователя

Status:
- успешно / частично / неуспешно

Task:
- TASK EXT 006 Shelter_2D — Синхронизировать manifest шаблона и правило unexpected `.obsidian`

Rules read:
- M:\GitHub\Shelter_2D\Shelter_2D\doc\Задача_внешнему_субагенту.md
- M:\GitHub\Shelter_2D\Shelter_2D\doc\Шаблон_агентской_системы\README_как_переносить.md
- M:\GitHub\Shelter_2D\Shelter_2D\doc\Шаблон_агентской_системы\MANIFEST_структуры.md
- M:\GitHub\Shelter_2D\Shelтер_2D\doc\Правила_Git_и_Review.md
- M:\GitHub\Shelter_2D\Shelter_2D\doc\Шаблон_агентской_системы\Шаблоны_документов\Правила_Git_и_Review.md

Scope:
- чтение: перечислить реально прочитанные файлы
- изменение: перечислить реально изменённые файлы

Mode:
- edit

Done:
- Синхронизирован порядок чтения в MANIFEST_структуры.md.
- Добавлено правило про служебные файлы как UNEXPECTED CHANGES в рабочие Git / Review правила.
- Добавлено такое же универсальное правило в шаблонные Git / Review правила.

Changed files:
- M:\GitHub\Shelter_2D\Shelter_2D\doc\Шаблон_агентской_системы\MANIFEST_структуры.md
- M:\GitHub\Shelter_2D\Shelter_2D\doc\Правила_Git_и_Review.md
- M:\GitHub\Shelter_2D\Shelter_2D\doc\Шаблон_агентской_системы\Шаблоны_документов\Правила_Git_и_Review.md
- M:\GitHub\Shelter_2D\Shelter_2D\doc\Задача_внешнему_субагенту.md

Checks:
- MANIFEST содержит 04_Dry_run_пример_переноса.md в порядке чтения.
- Рабочие Git / Review правила содержат правило про служебные файлы как UNEXPECTED CHANGES.
- Шаблонные Git / Review правила содержат такое же универсальное правило.
- Git не использовался.
- Новые файлы не создавались.
- UTF-8 сохранён.
- Кириллица читается нормально.

Problems:
- Нет / указать честно

For main Codex:
- Результат готов к проверке через GitHub после push.
```

Внимание: в отчёте не допускать опечатки в пути `Shelter_2D`.

---

## 9. USER SUMMARY в чат

После выполнения обязательно вернуть пользователю короткий отчёт в чат:

```text
USER SUMMARY

Выполнено:
- MANIFEST_структуры.md синхронизирован с README / Старт / Список_правил.
- В рабочие и шаблонные Git / Review правила добавлено правило про служебные файлы как UNEXPECTED CHANGES.
- Расширенный отчёт записан в БЛОК 2 файла Задача_внешнему_субагенту.md.

Изменено:
- перечислить файлы

Проблемы:
- Нет / указать

РАБОТА ЗАВЕРШЕНА
Итог: успешно
Контроль возвращён основному Codex.
```

Если задача выполнена частично или неуспешно, использовать:

```text
РАБОТА НЕ ЗАВЕРШЕНА
Итог: частично / неуспешно
Контроль возвращён основному Codex.
```

---

## 10. Если не можешь выполнить

Если нет права на запись, файл недоступен, инструмент редактирования не работает или scope непонятен:

- не имитировать успех;
    
- не писать `успешно`;
    
- не менять файлы наугад;
    
- вернуть `STATUS: неуспешно` или `STATUS: частично`;
    
- указать причину в `Problems`.

---

# БЛОК 2 — ПОСЛЕДНИЙ ОТЧЁТ АГЕНТА

AGENT REPORT

TASK:
- Добавить правило частого и экономного использования внешних агентов и локальных субагентов.

STATUS:
- Успешно.

DONE:
- В рабочем `Старт.md` добавлено короткое правило: перед нетривиальной задачей основной Codex оценивает, можно ли безопасно делегировать ограниченную подзадачу внешнему агенту или локальному субагенту.
- В рабочем `Правила_использования_субагентов.md` добавлен раздел `Правило экономного делегирования`.
- В рабочем `Список_правил.md` уточнено назначение правил субагентов: экономия ресурсов основного Codex, scope и отчётность.
- В шаблонных `Старт.md`, `Список_правил.md` и `Правила_использования_субагентов.md` добавлены зеркальные правила с placeholders и универсальной формулировкой.
- Закреплено: чаще использовать субагентов для read-only разведки, поиска, черновиков, независимой проверки, форматных проверок и подготовки вариантов.
- Закреплено: финальное решение, контроль scope, проверка результата и итоговый отчёт остаются за основным агентом.
- `БЛОК 1` оставлен очищенным, потому что задача выполнена успешно.

CHANGED:
- M:\GitHub\Shelter_2D\Shelter_2D\doc\Задача_агенту.md
- M:\GitHub\Shelter_2D\Shelter_2D\doc\Старт.md
- M:\GitHub\Shelter_2D\Shelter_2D\doc\Список_правил.md
- M:\GitHub\Shelter_2D\Shelter_2D\doc\Правила_использования_субагентов.md
- M:\GitHub\Shelter_2D\Shelter_2D\doc\Шаблон_агентской_системы\Шаблоны_документов\Старт.md
- M:\GitHub\Shelter_2D\Shelter_2D\doc\Шаблон_агентской_системы\Шаблоны_документов\Список_правил.md
- M:\GitHub\Shelter_2D\Shelter_2D\doc\Шаблон_агентской_системы\Шаблоны_документов\Правила_использования_субагентов.md

CREATED:
- Нет.

UNCHANGED:
- Правила_Git_и_Review.md
- Правила_создания_документов.md
- Настройка_прав_субагентов.md
- Задача_внешнему_субагенту.md
- Реальные конфиги Codex/OpenCode.
- `.git`
- `.obsidian`

UNEXPECTED CHANGES:
- Нет обнаруженных в рамках разрешённых проверок без Git.

UNTRACKED:
- Не проверялось, Git не использовался.

CHECKS:
- Проверено наличие правила `Правило экономного делегирования` в рабочем документе и в шаблоне.
- Проверено наличие короткого напоминания о делегировании в рабочем `Старт.md` и шаблонном `Старт.md`.
- Проверено, что `Список_правил.md` в рабочей и шаблонной ветке указывает экономию ресурсов как назначение правил субагентов.
- Проверено, что `БЛОК 1` очищен.
- UTF-8 сохранён.
- Кириллица читается нормально.
- Git не использовался.

GIT:
- Git не использовался.
- Git-команды чтения не выполнялись.
- commit не создавался агентом.
- push не выполнялся агентом.
- pull не выполнялся агентом.
- ветки не создавались и не переключались агентом.
- Git-история не изменялась агентом.

SUBAGENTS:
- Не использовались для этой микроправки: изменение было коротким, точечным и быстрее выполнялось основным Codex без запуска внешнего маршрута.

PROBLEMS:
- Нет.

NEXT:
- На следующих нетривиальных задачах основной Codex должен чаще выделять безопасные read-only, черновые или проверочные подзадачи для внешнего агента или локального субагента.

---

# БЛОК 3 — REVIEW / STATUS

## Текущий статус

Статус:
- Принято: `M:\GitHub\Shelter_2D\Shelter_2D\doc\Старт.md` является главным документом для первичного чтения.
- Принято: `M:\GitHub\Shelter_2D\Shelter_2D\doc\Задача_агенту.md` хранит активную задачу, последний отчёт и служебные правила агентского цикла.
- Принято: `M:\GitHub\Shelter_2D\Shelter_2D\doc` — строгая папка правил и задач агента.
- Принято: `M:\GitHub\Shelter_2D\Shelter_2D` — Obsidian Vault проекта.
- Принято: пользовательские Markdown-файлы называются на русском языке, если нет технической причины для другого имени.
- Принято: второй агент может готовить задачи для выполнения, но прямое сообщение пользователя имеет приоритет.

Baseline:
- Локальное состояние стартовых документов `Shelter_2D` после выполнения TASK CHAT 004.

Known limitations:
- История задач может содержать архивные упоминания старого шаблона, если они явно помечены как архив.
- Технические файлы Git, Obsidian или будущих инструментов могут иметь служебные англоязычные имена.

---

# ПРАВИЛО БОЛЬШОГО И МАЛОГО БЛОКА

Задачи могут приходить от пользователя напрямую или через второго агента, который готовит грамотную постановку задачи.

Задача второго агента не отменяет правила проекта и прямые сообщения пользователя.

## Большой блок

Большой блок:

- хранится в `M:\GitHub\Shelter_2D\Shelter_2D\doc\Задача_агенту.md`;
- остаётся основной задачей в документе;
- содержит цель, контекст, scope, разрешённые файлы, запреты и проверки;
- не переписывается заново только из-за поздних уточнений.

## Малый блок

Малый блок:

- отправляется агенту в чат;
- содержит команду прочитать актуальную задачу из `M:\GitHub\Shelter_2D\Shelter_2D\doc\Задача_агенту.md`;
- может содержать список дополнительных документов для чтения;
- может содержать уточнения, появившиеся после подготовки большого блока;
- может содержать критичные напоминания;
- дополняет большой блок, но не заменяет его.

Малый блок может быть написан пользователем или подготовлен вторым агентом.

Если уточнение появилось после подготовки большого блока и до отчёта агента, оно добавляется в малый блок без переписывания большого блока.

Если малый блок противоречит большому блоку, агент обязан остановиться и указать конфликт в отчёте.

Если задача второго агента противоречит прямому сообщению пользователя, приоритет имеет прямое сообщение пользователя.

---

# КОМПАКТНЫЙ ОТЧЁТ АГЕНТА

Последний отчёт агента хранится в `M:\GitHub\Shelter_2D\Shelter_2D\doc\Задача_агенту.md` в блоке `БЛОК 2 — ПОСЛЕДНИЙ ОТЧЁТ АГЕНТА`, ниже активной задачи.

Перед записью отчёта агент сверяет фактическое состояние файлов через разрешённые проверки. Если текущая задача запрещает Git, проверки выполняются без Git.

Если задача разрешает Git-проверки чтения и они показывают файл как изменённый, добавленный, удалённый или untracked, этот файл обязательно указывается в отчёте.

Файлы разделяются так:

- `CHANGED` — намеренно изменённые файлы в рамках задачи;
- `UNEXPECTED CHANGES` — побочные, автоматические или не входившие в задачу изменения;
- `UNTRACKED` — новые неотслеживаемые файлы, если Git-проверка была разрешена и выполнялась.

Побочные изменения нельзя скрывать. Если, например, `.obsidian\workspace.json` изменился автоматически из-за Obsidian, но задача не разрешала менять его намеренно, файл указывается в `UNEXPECTED CHANGES`, если изменение обнаружено в рамках разрешённых проверок.

Формат отчёта:

Любой review / проверка отчёта после выполненной задачи начинается с блока GIT / REPOSITORY CHECK.

Если Git не использовался, всё равно нужно явно написать:

```text
GIT / REPOSITORY CHECK

Branch:
- не проверялась

Status:
- не проверялся

Changed files:
- по отчёту агента

Diff summary:
- не проверялся

Commit:
- не создавался агентом

Push:
- не выполнялся агентом

Pull:
- не выполнялся агентом

GitHub check:
- не проверялся
```

```text
DONE:
- что сделано

CHANGED:
- намеренно изменённые файлы

UNEXPECTED CHANGES:
- побочные или автоматические изменения

UNTRACKED:
- новые неотслеживаемые файлы или \"не проверялось Git-командами\"

CHECKS:
- что проверено

GIT:
- использовался или не использовался
- commit не создавался агентом
- push не выполнялся агентом
- pull не выполнялся агентом
- ветки не создавались и не переключались агентом
- Git-история не изменялась агентом

PROBLEMS:
- реальные проблемы или риски

NEXT:
- следующий шаг
```

## Правило очистки активной задачи после отчёта

Если задача успешно выполнена, агент сначала записывает итоговый отчёт в `БЛОК 2 — ПОСЛЕДНИЙ ОТЧЁТ АГЕНТА`, а затем очищает `БЛОК 1 — ТЕКУЩАЯ ЗАДАЧА`.

После очистки `БЛОК 1` должен содержать только:

```text
# БЛОК 1 — ТЕКУЩАЯ ЗАДАЧА

Активной задачи нет.
Предыдущая задача выполнена.
Ожидается новая задача от пользователя.
```

Если задача выполнена частично, неуспешно, есть критичная ошибка или требуется решение пользователя, `БЛОК 1` не очищать. В этом случае агент обязан явно указать причину в `PROBLEMS`.

Очистка `БЛОКА 1` выполняется только после того, как отчёт записан в `БЛОК 2 — ПОСЛЕДНИЙ ОТЧЁТ АГЕНТА`.

---

# КАРТА ЧТЕНИЯ

Перед началом любой задачи читать:

```text
M:\GitHub\Shelter_2D\Shelter_2D\doc\Старт.md
M:\GitHub\Shelter_2D\Shelter_2D\doc\Задача_агенту.md
```

Тематическое чтение:

```text
Создание документов -> M:\GitHub\Shelter_2D\Shelter_2D\doc\Правила_создания_документов.md
Git / GitHub / проверка результата -> M:\GitHub\Shelter_2D\Shelter_2D\doc\Правила_Git_и_Review.md
Субагенты / делегирование / облачные и локальные агенты -> M:\GitHub\Shelter_2D\Shelter_2D\doc\Правила_использования_субагентов.md
Права субагентов / Codex / OpenCode -> M:\GitHub\Shelter_2D\Shelter_2D\doc\Настройка_прав_субагентов.md
Активная задача и последний отчёт -> M:\GitHub\Shelter_2D\Shelter_2D\doc\Задача_агенту.md
Стартовая карта проекта -> M:\GitHub\Shelter_2D\Shelter_2D\doc\Старт.md
Obsidian Vault проекта -> M:\GitHub\Shelter_2D\Shelter_2D
```

Агент читает только документы, нужные для текущей задачи.

Если нужного документа нет, указать это в отчёте.
